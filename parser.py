

class Parser(object):

    def __init__(self, tokens):
        # tokens things
        self.tokens = tokens
        # indexy shmexy
        self.token_index = 0


        def parse(self)

        while self.token_index < len(self.tokens):
